const { formatTopics } = require("./format-topics.utils.js");
const { formatUsers } = require("./format-users.utils.js");
const { formatArticles } = require("./format-articles.utils");
const { formatComments } = require("./format-comments.utils");

module.exports = {
  formatTopics,
  formatUsers,
  formatArticles,
  formatComments,
};
